#compdef _theme theme

colors=($HOME/.config/ghostty/themes/*(:t))

_theme() {
    local state line
    setopt localoptions globassign
    _arguments  '1: :->theme_name'
    case $state in
        theme_name)
            _values 'theme name' $colors
            ;;
    esac
}

_theme "$@"

