function aws-login {
    local profile=$1

    if [[ -z $profile ]]; then
        echo "Usage: aws-login <profile>" >&2
        return 1
    fi

    export AWS_PROFILE=$profile
    aws sso login --profile "$profile"
}
